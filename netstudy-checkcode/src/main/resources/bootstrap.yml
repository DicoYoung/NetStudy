spring:
  application:
    name: checkcode
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:
        namespace: dev-ns
        group: netstudy
      config:
        namespace: dev-ns
        group: netstudy
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: swagger-${spring.profiles.active}.yaml
            group: netstudy-common
            refresh: true
          - data-id: logging-${spring.profiles.active}.yaml
            group: netstudy-common
            refresh: true
          - data-id: redis-${spring.profiles.active}.yaml
            group: netstudy-common
            refresh: true
